# Core dependencies with reduced complexity
numpy>=1.24.0
gradio>=4.36.0
opencv-python>=4.9.0
onnx>=1.16.0
insightface>=0.7.3
psutil>=5.9.0
pillow>=10.0.0

# ONNX Runtime (version depends on platform)
onnxruntime>=1.18.0; sys_platform == 'darwin' and platform_machine != 'arm64'
onnxruntime-silicon>=1.16.0; sys_platform == 'darwin' and platform_machine == 'arm64'
onnxruntime-gpu>=1.18.0; sys_platform != 'darwin'

# Core ML and AI libraries
protobuf>=4.25.0
tqdm>=4.66.0
ftfy>=6.1.0
regex>=2023.0.0

# Face enhancement libraries (optional, lighter versions)
gfpgan>=1.3.8
realesrgan>=0.3.0

# Enhanced processing libraries
scikit-image>=0.22.0
scipy>=1.11.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9

# Basic HTTP client
requests>=2.31.0

# Basic monitoring
nvidia-ml-py>=12.535.0; sys_platform == 'linux'
